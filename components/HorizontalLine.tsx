import React from 'react';
import styled from 'styled-components/native';

const Line = styled.Text`
  height: 1px;
  background-color: rgba(45, 167, 113, 0.5);
`;

const HorizontalLine = () => {
  return <Line> </Line>;
};

export default HorizontalLine;
